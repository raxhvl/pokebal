[project]
name = "beetle"
version = "0.1.0"
description = "A/B test BALs."
requires-python = ">=3.14"
dependencies = []

[project.scripts]
beetle = "cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = ["src"]
sources = ["src"]
